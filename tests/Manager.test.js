const Manager = require("../lib/manager");
const manager = require("../lib/manager");
test("manager", () => {
    const managerName = new employee();
    expect(typeof(managerName)).toBe("object");
});