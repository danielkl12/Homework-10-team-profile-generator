const Engineer = require("../lib/engineer");
const engineer = require("../lib/engineer");
test("engineer", () => {
    const engineerName = new employee();
    expect(typeof(engineerName)).toBe("object");
});